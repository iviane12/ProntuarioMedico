spring.application.name=pwebII

# Configurações do PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/projetoweb2
spring.datasource.username=postgres
spring.datasource.password=2613

# Configurações da JPA
# O 'update' faz com que a JPA crie ou atualize as tabelas automaticamente!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true